// -----------------------Number-----------------------

const score = 100
console.log(score);

const balance = new Number(298.6547)         // Will give additional functions or properties
console.log(balance);

console.log(balance.toString());        // Convert the number into String

console.log(balance.toString().length);     // Now, it is a string and we can use string properties on it.

console.log(balance.toFixed(3));        //  Rounds the string to a specified number of decimals.


console.log(balance.toPrecision(3));














